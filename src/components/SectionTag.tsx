const SectionTag = ({ children }: { children: string }) => (
  <span className="inline-block font-mono text-xs uppercase tracking-[0.2em] text-green font-medium mb-4">
    {children}
  </span>
);

export default SectionTag;
